{
  "name": "qworker",
  "version": "0.9.1",
  "main": "qworker.js",
  "description": "quick work script runner",
  "license": "Apache-2.0",
  "keywords": ["worker", "queue", "script", "runner"],
  "engines": {
    "node": ">=0.10"
  },

  "author": "andrasq",
  "repository": { "type": "git", "url": "git://github.com/andrasq/node-qworker" },
  "readme": "see README.md in the repo",

  "dependencies": {
    "qfslock": "0.9.0",
    "qinvoke": "0.11.3",
    "quickq": "0.10.2"
  },
  "devDependencies": {
    "qnit": "0.24.0"
  },

  "scripts": {
    "test": "qnit -t 10000 test/test*",
    "coverage": "nyc --reporter text --reporter lcov npm test",
    "clean": "rm -rf .nyc_output coverage"
  }
}
